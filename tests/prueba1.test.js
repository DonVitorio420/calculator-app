import { describe, it, expect } from 'vitest';

describe('Prueba inicial de entorno', () => {
  it('Verifica que el entorno de pruebas funciona correctamente', () => {
    expect(true).toBe(true);
  });
});
